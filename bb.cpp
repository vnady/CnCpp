#include <iostream>
#include <string>
#include <vector>

using namespace std;
/*
int Comb(int m, int n)
{
    if(1 == n)
    {
        return m;
    }
    else if(m == n)
    {
        return 1;
    }

    return Comb(m-1, n)+Comb(m-1, n-1);
}
*/

int main()
{
    string str="bafahfia";
    cout <<std::sort()<<endl;
   return 0;
}
